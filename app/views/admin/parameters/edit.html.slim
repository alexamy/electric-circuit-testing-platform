.block
  = render 'shared/errors', resource: @question

.block
  p.italic = Question.human_attribute_name('formula_text')
  p = simple_format @question.formula_text

= form_with model: [:admin, @question], url: admin_question_parameters_path(@question) do |form|
  .flex.flex-row.flex-wrap
    = form.fields_for :parameters do |parameter|
      = render partial: 'step_parameter', object: parameter

  .form-actions
    = form.submit 'Сохранить параметры', class: 'button'

.mt-4
  = link_to 'Перейти к вопросу', admin_question_path(@question), class: 'link'
