$('.question-errors').html('<%= render 'shared/errors', resource: @question %>');
$('.alert').html('');

<% if @alert.present? %>

  $('.alert').html('<%= j @alert %>');

<% else %>

  $('.question').html('<%= j render('form') %>')

<% end %>
